import React, {useState, useEffect} from 'react'

import styles from './RoundUI.module.css';

const RoundUI = (props) => {

  let classes = [styles.RoundUI];

  return (
    <div className={classes.join(" ")}>

    </div>
  )
}

export default RoundUI
